apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: ontopic-postgres
spec:
  entryPoints:
    - postgres
  routes:
    - match: HostSNI(`*`)
      services:
        - name: ontopic-server-postgres
          port: 4300

