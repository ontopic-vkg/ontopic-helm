apiVersion: v1
kind: Service
metadata:
  name: {{ include "ontopic-server.fullname" . }}
  labels:
    {{- include "ontopic-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ontopic-server.selectorLabels" . | nindent 4 }}
